import styles from './{{name}}.module.scss';
import type { Types } from './{{name}}.types';

const {{pascalCase name}} = ({  }: Types) => {
  return (
    <section className={styles['{{pascalCase name}}']}>

    </section>
  );
};

export default {{pascalCase name}};